name: lecturas
description: Pequeña app para leer las lecturas del dia
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1
environment:
  sdk: ">=2.16.2 <3.0.0"

dependencies:
  http:
  intl:
  provider: ^6.0.2
  shared_preferences: ^2.0.13
  json_theme: ^3.3.1
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  html: ^0.15.0
  flutter_localizations: 
    sdk: flutter
    version: ^0.0.0
  
flutter_icons:
  android: true
  ios: false
  image_path_android: assets/bible_icon.png
  image_path_ios: assets/bible_icon.png
  adaptive_icon_background: assets/bible_icon_bg.png
  adaptive_icon_foreground: assets/bible_icon_fg.png

dev_dependencies:
  flutter_launcher_icons:
    git:
      url: https://github.com/Davenchy/flutter_launcher_icons.git
      ref: fixMinSdkParseFlutter2.8
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  assets:
    # Add assets from the images directory to the application.
    - assets/theme.json